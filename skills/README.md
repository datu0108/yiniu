# Agent Skills

每个 Skill 独立目录，符合 Accio Work 封装规范。

| Skill | 目录 | 说明 |
|-------|------|------|
| ImageEditor | `image_editor_api/` | gpt-image-2 图片编辑 API |

安装到 Agent 时，将 `skills/` 复制或链接到 `${agent_core}/skills/`。
